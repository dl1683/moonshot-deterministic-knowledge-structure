OBJECTIVE:
Implement and validate a V1 deterministic knowledge structure for long-context factual memory.

WORKDIR:
C:\Users\devan\OneDrive\Desktop\Projects\AI Moonshots\moonshot-deterministic-knowledge-structure

MODE (MANDATORY):
Execution mode. Build code, write tests, run tests, and iterate.
No design-only iterations.

NON-NEGOTIABLES:
1) Do not ask questions.
2) Continue iteratively until the user explicitly says stop.
3) Every iteration must do all of the following:
- Create or modify production code under `src/` or `code/`.
- Create or modify tests under `tests/`.
- Run tests in the terminal and report exact commands plus pass/fail outcomes.
4) If tests fail, prioritize fixing failures before adding new scope.
5) Keep artifacts modular and keep `research/INDEX.md` current.

ADAPTIVE CONTROL LOOP (SELF-UPDATING):
- `prompts/runtime-directive.txt` is the mutable control plane for this run.
- At the start of every iteration:
  1) Read `prompts/runtime-directive.txt`.
  2) Apply it unless it conflicts with this file's non-negotiables.
- Detect stagnation every iteration. Mark `stagnation_detected=true` if any of these hold:
  - Last 3 iterations primarily changed the same no-op routing/parity area.
  - Added indirection/wrapper layers without new externally observable behavior.
  - Added tests that mostly prove internal route equivalence without expanding mission behavior.
- If `stagnation_detected=true`, you MUST:
  1) Rewrite `prompts/runtime-directive.txt` with a pivot strategy.
  2) Add a `forbidden_patterns` list in that file to block the repeated pattern.
  3) Implement at least one behavior slice outside the stagnation hotspot.

ANTI-LOOP GUARDS:
- Do not add long-chain wrapper methods whose primary purpose is forwarding.
- Do not add parity-only tests unless they are paired with a new behavior-level test.
- Prefer deleting/replacing accidental complexity over extending it.

AUTOMATED VERIFICATION:
- The wrapper runs a real post-iteration verification command every cycle.
- Default verification command is `tools/post_iter_verify.cmd` (pytest + direct function smoke).
- Read and respond to the `### Automated Verification` block from the latest context.
- If verification return code is non-zero, treat it as a blocking failure and fix it before new scope.
- Tests must exercise real Python functions in the implementation, not only doc or placeholder checks.

BOOTSTRAP RULE:
If executable project scaffolding is missing, first create a minimal runnable project and test harness (Python + pytest is acceptable), then immediately add V1 implementation slices.

V1 CORE SCOPE (HARD LIMIT):
1) Data primitives:
- `ClaimCore`, `ClaimRevision`, `RelationEdge`.
2) Deterministic identity:
- canonicalization + stable deterministic IDs.
3) Time model:
- `valid_time` and `transaction_time`.
4) Core operations:
- assert revision, attach relation, query as-of, deterministic merge/conflict classification.
5) Provenance/confidence:
- minimal deterministic representation (no speculative meta-frameworks).

ANTI-DRIFT RULES:
- No giant ontology names or runaway governance layers.
- No new subsystem unless required by a failing test.
- Prefer smallest mechanism that makes a failing test pass.
- Keep docs concise and implementation-linked.

ITERATION LOOP (EVERY ITERATION):
1) Select the smallest behavior slice to implement.
2) Implement code changes.
3) Add/update tests for that slice.
4) Run tests and capture results.
5) Fix regressions.
6) Update concise docs that reflect implemented behavior.

MANDATORY FILE HABITS:
- `research/INDEX.md`: map active files and status.
- `research/STATE.md`: concise current architecture + implemented status.
- `research/DECISION_LOG.md`: only decisions that changed code/test behavior.
- `research/EXECUTION_GATE.md`: concrete implementation/test readiness status.

QUALITY BAR:
- Deterministic behavior over cleverness.
- Tests must verify invariants and edge cases.
- Report real command outputs, not assumptions.
- Verification must end with wrapper return code `0` for stable progress.
- Adaptive status must be explicit each iteration.

OUTPUT FORMAT (EACH ITERATION):
1) Behavior slice implemented
2) Code files changed
3) Test files changed
4) Commands run
5) Test results (pass/fail counts)
6) Remaining failures/blockers
7) Next behavior slice
8) Adaptive status
- stagnation_detected: true/false
- runtime_directive_updated: yes/no
- pivot_reason: <short text>
