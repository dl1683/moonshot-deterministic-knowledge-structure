RUNTIME DIRECTIVE (MUTABLE)

Current tactical objective:
- Deliver externally observable V1 behavior slices in merge/conflict queries and relation lifecycle queries.

Current focus:
- Add queryable deterministic state for pending/active relation lifecycle across tx cutoffs.
- Add behavior-level assertions that validate merge conflict projections against real merge streams.
- Remove or avoid no-op routing expansions unless a failing behavior test requires them.

forbidden_patterns:
- Editing deep dispatch/parity chain methods when no user-visible behavior changes.
- Adding wrapper/forwarder helpers with no new semantic output.
- Adding tests that only compare equivalent internal routes.
- Expanding method-name indirection trees.

Pivot trigger:
- If any 2 consecutive iterations land in no-op routing/parity edits, pivot immediately to one merge or relation lifecycle behavior that changes query outputs.
